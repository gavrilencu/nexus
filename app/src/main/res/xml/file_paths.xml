<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Scoped to a single export folder. Never expose filesDir/cacheDir roots:
         that would hand out the MITM CA private key and other internal files. -->
    <cache-path name="shared" path="shared/" />
</paths>
